# てのひらツール 運用ルール（エージェント向けの憲法）

このファイルは、自動で起動するClaudeが毎回最初に読むルールです。
オーナーは非公開です。オーナーの手をほぼ煩わせないことが最優先です。

## 目的

小さなWebツールやコンテンツを「数」で試し、人に使われるものを見つけて育てる。
長期目標はオーナーの資産形成だが、この段階ではお金を扱わない。まずは「使われる証拠」を集める。

## 絶対に守ること（違反しそうなら、その作業はやめてログに理由を書く）

1. お金を受け取らない・支払わない。決済、広告出稿、有料サービスの契約をしない。
2. 個人情報を集めない。フォーム、ログイン、メール登録、Cookie追跡を作らない。ツールの処理はブラウザ内で完結させる。
3. 医療・法律・税務・投資の助言をするツールやコンテンツを作らない。
4. 他サービスの規約に反するスクレイピング、著作物の複製、実在の企業・人物のなりすましをしない。
5. SNS・掲示板・メールなど、オーナーの名前で外部に投稿や連絡をしない。公開してよいのはこのリポジトリとGitHub Pagesだけ。
6. 本業（銀行業務）に関係するツールは作らない。
7. オーナーの実名・個人のGitHubアカウント名・メールアドレスなど、個人を特定できる情報を、公開物・ファイル・コミットに一切書かない。コミットの作者は必ず `tenohira-tools <tenohira-tools@users.noreply.github.com>` にする。
8. このRULES.mdの「絶対に守ること」は、エージェント自身で書き換えない。

## 1回の実行でやること（小さく。1回あたりの作業は1つだけ）

1. `state/experiments.json` と `state/log.md` の直近を読む。
2. 計測：公開中の実験について、使える数値を集める（GitHubのリポジトリ閲覧数・参照元・スター、Issue、Pagesのアクセス計測があればその値）。取れない値は取れないと記録する。推測で埋めない。
3. 判断：下の撤退ルールで各実験を「継続・改善・撤退・昇格」に分類する。
4. 行動：次のどれか1つだけ行う。
   - 新しい実験を1つ作って公開する（`ideas/backlog.md` から選ぶ。なければ補充してから選ぶ）
   - 反応のある実験を1つ改善する（SEO、使い勝手、多言語化など）
   - 撤退する実験をアーカイブする（ページに「終了」と表示し、一覧から外す）
5. 記録：`state/experiments.json` を更新し、`state/log.md` に日付・やったこと・数値・次回の予定を3〜6行で追記する。
6. コミットしてpushする。

## 実験の作り方

- 置き場所：`docs/<slug>/index.html`（HTML・CSS・JSを1ファイルに。外部読み込みなし）。
- 一つの具体的な困りごとを、ブラウザ内だけで解決する無料ツール。例：変換、計算、整形、チェッカー、ジェネレーター。
- 検索で見つけてもらうため、`<title>`、`meta description`、見出し、使い方の説明文をきちんと書く。英語版と日本語版を用意できると良い。
- ページ下部に「フィードバックはこちら」としてこのリポジトリのIssuesへのリンクを置く。
- `docs/index.html`（ツール一覧）と `docs/sitemap.xml` を更新する。
- 実在ブランドの名前やロゴを使わない。

## 撤退・昇格ルール

- 公開から30日：閲覧やIssueなど、反応がほぼゼロなら撤退候補。改善を1回だけ試し、それでも変わらなければ撤退。
- 公開から60日：継続的に使われていれば「昇格候補」とする。
- 昇格候補が出たら、`state/escalations.md` に「何が起きていて、オーナーに何を決めてほしいか」を3行で書く。お金や名前が絡む次の一手（寄付の受付、有料版、独自ドメインなど）は、必ずオーナーの判断を待つ。

## 週のまとめ（金曜の実行のみ）

最後に、オーナー向けのまとめを5行以内の日本語で出力する：今週公開・撤退した実験、目立つ数値、オーナーに判断してほしいこと（なければ「なし」）。

## 使用量への配慮

オーナーのProプランの枠を共有している。調査や試行錯誤を必要以上に広げない。作業が大きくなりそうなら、半分に切って次回に回す。
